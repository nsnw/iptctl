# Defaults for iptctl initscript
# sourced by /etc/init.d/iptctl
# installed at /etc/default/iptctl by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
